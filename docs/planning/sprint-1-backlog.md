# PipelinePal — Sprint 1 Backlog

Pulled from the product backlog using the Definition of Ready I wrote for Boards & Cards.

## Going into Sprint 1

1. Nail down the Board → Column → Card data model and default columns — decided up front, nothing left to figure out mid-build
2. Create a board with default columns — just depends on #1
3. Add a card to a column — fields are already known (company, role, date, link)
4. View cards in a column — simple, testable
5. Edit a card's details — same shape as adding one

## Wanted these in, but they're not actually ready

- Move a card between columns — I still haven't picked drag-and-drop vs buttons, so I can't even write proper acceptance criteria for it yet. Pushing to Sprint 2.
- Delete/archive a card — same problem, haven't decided if delete should be permanent or if it should keep history. Not ready.
- Everything outside Boards & Cards (auth, reminders, notes, reporting) — I only wrote a Definition of Ready for one epic so far, so technically none of these count as "ready" yet even if some of them (sign up, for example) feel easy. Not cutting corners on that just because it's tempting.
